<button type="button" class="button"> <%= link_to "Add new data", new_fact_path(@fact) %></button>
<hr>
<% @tables.each do |table| %>
<%= table[0].title.name %>
<table>
  <tr>
    <td>Country</td>
    <td>Data</td>
    <td>Year</td>
  </tr>
  <% table.each do |fact| %>
  <tr>
    <td><%= fact.location %></td>
    <td><%= fact.data %></td>
    <td><%= fact.year.year %>
  </tr>
  <% end %>
</table>
<% end %>
